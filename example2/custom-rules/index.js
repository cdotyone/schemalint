const requiredField = require('./requiredField');
const lastField = require('./lastField');
const fieldType = require('./fieldType');
const renameField = require('./renameField');

module.exports = {
  requiredField,
  lastField,
  fieldType,
  renameField,
};
